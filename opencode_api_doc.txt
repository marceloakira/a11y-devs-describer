================================================================================
  OpenCode API Documentation
  OpenAPI Version: 3.1.0
  API Title: opencode
  API Version: 1.0.0
  Description: opencode api
================================================================================

ENDPOINTS
================================================================================

  /agent
  ~~~~~~

    [GET] app.agents
    Tags: instance
    Summary: List agents
    Description: Get a list of all available AI agents in the OpenCode system.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of agents
Array<Agent>

  ------------------------------------------------------------

  /api/model
  ~~~~~~~~~~

    [GET] v2.model.list
    Tags: v2 models
    Summary: List v2 models
    Description: Retrieve available v2 models ordered by release date.
    Parameters:
      - location (query): object
    Responses:
      200: Success
Array<ModelV2Info>

  ------------------------------------------------------------

  /api/provider
  ~~~~~~~~~~~~~

    [GET] v2.provider.list
    Tags: v2 providers
    Summary: List v2 providers
    Description: Retrieve active v2 AI providers so clients can show provider availability and configuration.
    Parameters:
      - location (query): object
    Responses:
      200: Success
Array<ProviderV2Info>

  ------------------------------------------------------------

  /api/provider/{providerID}
  ~~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] v2.provider.get
    Tags: v2 providers
    Summary: Get v2 provider
    Description: Retrieve a single v2 AI provider so clients can inspect its availability and endpoint settings.
    Parameters:
      - providerID (path): string (required)
      - location (query): object
    Responses:
      200: ProviderV2.Info
ProviderV2Info
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /api/session
  ~~~~~~~~~~~~

    [GET] v2.session.list
    Tags: v2
    Summary: List v2 sessions
    Description: Retrieve sessions in the requested order. Items keep that order across pages; use cursor.next or cursor.previous to move through the ordered list.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - limit (query): number
      - order (query): string [asc, desc]
      - path (query): string
      - roots (query): any
      - start (query): number
      - search (query): string
      - cursor (query): string
    Responses:
      200: V2SessionsResponse
V2SessionsResponse
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /api/session/{sessionID}/compact
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] v2.session.compact
    Tags: v2
    Summary: Compact v2 session
    Description: Compact a v2 session conversation.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      204: <No Content>

  ------------------------------------------------------------

  /api/session/{sessionID}/context
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] v2.session.context
    Tags: v2
    Summary: Get v2 session context
    Description: Retrieve the active context messages for a v2 session (all messages after the last compaction).
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Success
Array<SessionMessage>

  ------------------------------------------------------------

  /api/session/{sessionID}/message
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] v2.session.messages
    Tags: v2 messages
    Summary: Get v2 session messages
    Description: Retrieve projected v2 messages for a session. Items keep the requested order across pages; use cursor.next or cursor.previous to move through the ordered timeline.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
      - limit (query): number
      - order (query): string [asc, desc]
      - cursor (query): string
    Responses:
      200: V2SessionMessagesResponse
V2SessionMessagesResponse
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /api/session/{sessionID}/prompt
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] v2.session.prompt
    Tags: v2
    Summary: Send v2 message
    Description: Create a v2 session message and queue it for the agent loop.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        delivery: SessionDelivery
        prompt *: Prompt
      }
    Responses:
      200: Session.Message
SessionMessage

  ------------------------------------------------------------

  /api/session/{sessionID}/wait
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] v2.session.wait
    Tags: v2
    Summary: Wait for v2 session
    Description: Wait for a v2 session agent loop to become idle.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      204: <No Content>

  ------------------------------------------------------------

  /auth/{providerID}
  ~~~~~~~~~~~~~~~~~~

    [PUT] auth.set
    Tags: control
    Summary: Set auth credentials
    Description: Set authentication credentials
    Parameters:
      - providerID (path): string (required)
    Request Body:
Auth
    Responses:
      200: Successfully set authentication credentials
boolean
      400: Bad request
BadRequestError

    [DELETE] auth.remove
    Tags: control
    Summary: Remove auth credentials
    Description: Remove authentication credentials
    Parameters:
      - providerID (path): string (required)
    Responses:
      200: Successfully removed authentication credentials
boolean
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /command
  ~~~~~~~~

    [GET] command.list
    Tags: instance
    Summary: List commands
    Description: Get a list of all available commands in the OpenCode system.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of commands
Array<Command>

  ------------------------------------------------------------

  /config
  ~~~~~~~

    [GET] config.get
    Tags: config
    Summary: Get configuration
    Description: Retrieve the current OpenCode configuration settings and preferences.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Get config info
Config

    [PATCH] config.update
    Tags: config
    Summary: Update configuration
    Description: Update OpenCode configuration settings and preferences.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Config
    Responses:
      200: Successfully updated config
Config
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /config/providers
  ~~~~~~~~~~~~~~~~~

    [GET] config.providers
    Tags: config
    Summary: List config providers
    Description: Get a list of all configured AI providers and their default models.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of providers
Object {
          default *: Object{}
          providers *: Array<Provider>
        }

  ------------------------------------------------------------

  /event
  ~~~~~~

    [GET] event.subscribe
    Tags: event
    Summary: Subscribe to events
    Description: Get events
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Event stream

  ------------------------------------------------------------

  /experimental/console
  ~~~~~~~~~~~~~~~~~~~~~

    [GET] experimental.console.get
    Tags: experimental
    Summary: Get active Console provider metadata
    Description: Get the active Console org name and the set of provider IDs managed by that Console org.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Active Console provider metadata
ConsoleState
      500: InternalServerError
effect_HttpApiError_InternalServerError

  ------------------------------------------------------------

  /experimental/console/orgs
  ~~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] experimental.console.listOrgs
    Tags: experimental
    Summary: List switchable Console orgs
    Description: Get the available Console orgs across logged-in accounts, including the current active org.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Switchable Console orgs
Object {
          orgs *: Array<Object {
            accountEmail *: string
            accountID *: string
            accountUrl *: string
            active *: boolean
            orgID *: string
            orgName *: string
          }>
        }
      500: InternalServerError
effect_HttpApiError_InternalServerError

  ------------------------------------------------------------

  /experimental/console/switch
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] experimental.console.switchOrg
    Tags: experimental
    Summary: Switch active Console org
    Description: Persist a new active Console account/org selection for the current local OpenCode state.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        accountID *: string
        orgID *: string
      }
    Responses:
      200: Switch success
boolean

  ------------------------------------------------------------

  /experimental/resource
  ~~~~~~~~~~~~~~~~~~~~~~

    [GET] experimental.resource.list
    Tags: experimental
    Summary: Get MCP resources
    Description: Get all available MCP resources from connected servers. Optionally filter by name.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: MCP resources
Object{}

  ------------------------------------------------------------

  /experimental/session
  ~~~~~~~~~~~~~~~~~~~~~

    [GET] experimental.session.list
    Tags: experimental
    Summary: List sessions
    Description: Get a list of all OpenCode sessions across projects, sorted by most recently updated. Archived sessions are excluded by default.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - roots (query): any
      - start (query): number
      - cursor (query): number
      - search (query): string
      - limit (query): number
      - archived (query): any
    Responses:
      200: List of sessions
Array<GlobalSession>

  ------------------------------------------------------------

  /experimental/tool
  ~~~~~~~~~~~~~~~~~~

    [GET] tool.list
    Tags: experimental
    Summary: List tools
    Description: Get a list of available tools with their JSON schema parameters for a specific provider and model combination.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - provider (query): string (required)
      - model (query): string (required)
    Responses:
      200: Tools
ToolList
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /experimental/tool/ids
  ~~~~~~~~~~~~~~~~~~~~~~

    [GET] tool.ids
    Tags: experimental
    Summary: List tool IDs
    Description: Get a list of all available tool IDs, including both built-in tools and dynamically registered tools.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Tool IDs
ToolIDs
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /experimental/workspace
  ~~~~~~~~~~~~~~~~~~~~~~~

    [GET] experimental.workspace.list
    Tags: workspace
    Summary: List workspaces
    Description: List all workspaces.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Workspaces
Array<Workspace>

    [POST] experimental.workspace.create
    Tags: workspace
    Summary: Create workspace
    Description: Create a workspace for the current project.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        branch: Union(string, null)
        extra: Union(Object{}, null)
        id: string
        type *: string
      }
    Responses:
      200: Workspace created
Workspace
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /experimental/workspace/adapter
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] experimental.workspace.adapter.list
    Tags: workspace
    Summary: List workspace adapters
    Description: List all available workspace adapters for the current project.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Workspace adapters
Array<Object {
          description *: string
          name *: string
          type *: string
        }>

  ------------------------------------------------------------

  /experimental/workspace/status
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] experimental.workspace.status
    Tags: workspace
    Summary: Workspace status
    Description: Get connection status for workspaces in the current project.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Workspace status
Array<Object {
          status *: Enum[connected, connecting, disconnected, error]
          workspaceID *: string
        }>

  ------------------------------------------------------------

  /experimental/workspace/sync-list
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] experimental.workspace.syncList
    Tags: workspace
    Summary: Sync workspace list
    Description: Register missing workspaces returned by workspace adapters.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      204: Workspace list synced

  ------------------------------------------------------------

  /experimental/workspace/warp
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] experimental.workspace.warp
    Tags: workspace
    Summary: Warp session into workspace
    Description: Move a session's sync history into the target workspace, or detach it to the local project.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        copyChanges: boolean
        id *: Union(string, null)
        sessionID *: string
      }
    Responses:
      204: Session warped
      400: WorkspaceWarpError | VcsApplyError
Union(WorkspaceWarpError, VcsApplyError)

  ------------------------------------------------------------

  /experimental/workspace/{id}
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [DELETE] experimental.workspace.remove
    Tags: workspace
    Summary: Remove workspace
    Description: Remove an existing workspace.
    Parameters:
      - id (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Workspace removed
Workspace
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /experimental/worktree
  ~~~~~~~~~~~~~~~~~~~~~~

    [GET] worktree.list
    Tags: experimental
    Summary: List worktrees
    Description: List all sandbox worktrees for the current project.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of worktree directories
Array<string>
      400: WorktreeError
WorktreeError

    [POST] worktree.create
    Tags: experimental
    Summary: Create worktree
    Description: Create a new git worktree for the current project and run any configured startup scripts.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
WorktreeCreateInput
    Responses:
      200: Worktree created
Worktree
      400: WorktreeError
WorktreeError

    [DELETE] worktree.remove
    Tags: experimental
    Summary: Remove worktree
    Description: Remove a git worktree and delete its branch.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
WorktreeRemoveInput
    Responses:
      200: Worktree removed
boolean
      400: WorktreeError
WorktreeError

  ------------------------------------------------------------

  /experimental/worktree/reset
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] worktree.reset
    Tags: experimental
    Summary: Reset worktree
    Description: Reset a worktree branch to the primary default branch.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
WorktreeResetInput
    Responses:
      200: Worktree reset
boolean
      400: WorktreeError
WorktreeError

  ------------------------------------------------------------

  /file
  ~~~~~

    [GET] file.list
    Tags: file
    Summary: List files
    Description: List files and directories in a specified path.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - path (query): string (required)
    Responses:
      200: Files and directories
Array<FileNode>

  ------------------------------------------------------------

  /file/content
  ~~~~~~~~~~~~~

    [GET] file.read
    Tags: file
    Summary: Read file
    Description: Read the content of a specified file.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - path (query): string (required)
    Responses:
      200: File content
FileContent

  ------------------------------------------------------------

  /file/status
  ~~~~~~~~~~~~

    [GET] file.status
    Tags: file
    Summary: Get file status
    Description: Get the git status of all files in the project.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: File status
Array<File>

  ------------------------------------------------------------

  /find
  ~~~~~

    [GET] find.text
    Tags: file
    Summary: Find text
    Description: Search for text patterns across files in the project using ripgrep.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - pattern (query): string (required)
    Responses:
      200: Matches
Array<Object {
          absolute_offset *: integer
          line_number *: integer
          lines *: Object {
            text *: string
          }
          path *: Object {
            text *: string
          }
          submatches *: Array<Object {
            end *: integer
            match *: Object {
              text *: string
            }
            start *: integer
          }>
        }>

  ------------------------------------------------------------

  /find/file
  ~~~~~~~~~~

    [GET] find.files
    Tags: file
    Summary: Find files
    Description: Search for files or directories by name or pattern in the project directory.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - query (query): string (required)
      - dirs (query): string [true, false]
      - type (query): string [file, directory]
      - limit (query): integer
    Responses:
      200: File paths
Array<string>

  ------------------------------------------------------------

  /find/symbol
  ~~~~~~~~~~~~

    [GET] find.symbols
    Tags: file
    Summary: Find symbols
    Description: Search for workspace symbols like functions, classes, and variables using LSP.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - query (query): string (required)
    Responses:
      200: Symbols
Array<Symbol>

  ------------------------------------------------------------

  /formatter
  ~~~~~~~~~~

    [GET] formatter.status
    Tags: instance
    Summary: Get formatter status
    Description: Get formatter status
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Formatter status
Array<FormatterStatus>

  ------------------------------------------------------------

  /global/config
  ~~~~~~~~~~~~~~

    [GET] global.config.get
    Tags: global
    Summary: Get global configuration
    Description: Retrieve the current global OpenCode configuration settings and preferences.
    Responses:
      200: Get global config info
Config

    [PATCH] global.config.update
    Tags: global
    Summary: Update global configuration
    Description: Update global OpenCode configuration settings and preferences.
    Request Body:
Config
    Responses:
      200: Successfully updated global config
Config
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /global/dispose
  ~~~~~~~~~~~~~~~

    [POST] global.dispose
    Tags: global
    Summary: Dispose instance
    Description: Clean up and dispose all OpenCode instances, releasing all resources.
    Responses:
      200: Global disposed
boolean

  ------------------------------------------------------------

  /global/event
  ~~~~~~~~~~~~~

    [GET] global.event
    Tags: global
    Summary: Get global events
    Description: Subscribe to global events from the OpenCode system using server-sent events.
    Responses:
      200: Event stream

  ------------------------------------------------------------

  /global/health
  ~~~~~~~~~~~~~~

    [GET] global.health
    Tags: global
    Summary: Get health
    Description: Get health information about the OpenCode server.
    Responses:
      200: Health information
Object {
          healthy *: boolean
          version *: string
        }

  ------------------------------------------------------------

  /global/upgrade
  ~~~~~~~~~~~~~~~

    [POST] global.upgrade
    Tags: global
    Summary: Upgrade opencode
    Description: Upgrade opencode to the specified version or latest if not specified.
    Request Body:
Object {
        target: string
      }
    Responses:
      200: Upgrade result
Union(Object {
          success *: boolean
          version *: string
        }, Object {
          error *: string
          success *: boolean
        })
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /instance/dispose
  ~~~~~~~~~~~~~~~~~

    [POST] instance.dispose
    Tags: instance
    Summary: Dispose instance
    Description: Clean up and dispose the current OpenCode instance, releasing all resources.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Instance disposed
boolean

  ------------------------------------------------------------

  /log
  ~~~~

    [POST] app.log
    Tags: control
    Summary: Write log
    Description: Write a log entry to the server logs with specified level and metadata.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        extra: Object{}
        level *: Enum[debug, info, error, warn]  # Log level
        message *: string  # Log message
        service *: string  # Service name for the log entry
      }
    Responses:
      200: Log entry written successfully
boolean
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /lsp
  ~~~~

    [GET] lsp.status
    Tags: instance
    Summary: Get LSP status
    Description: Get LSP server status
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: LSP server status
Array<LSPStatus>

  ------------------------------------------------------------

  /mcp
  ~~~~

    [GET] mcp.status
    Tags: mcp
    Summary: Get MCP status
    Description: Get the status of all Model Context Protocol (MCP) servers.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: MCP server status
Object{}

    [POST] mcp.add
    Tags: mcp
    Summary: Add MCP server
    Description: Dynamically add a new Model Context Protocol (MCP) server to the system.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        config *: Union(McpLocalConfig, McpRemoteConfig)
        name *: string
      }
    Responses:
      200: MCP server added successfully
Object{}
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /mcp/{name}/auth
  ~~~~~~~~~~~~~~~~

    [POST] mcp.auth.start
    Tags: mcp
    Summary: Start MCP OAuth
    Description: Start OAuth authentication flow for a Model Context Protocol (MCP) server.
    Parameters:
      - name (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: OAuth flow started
Object {
          authorizationUrl *: string
          oauthState *: string
        }
      400: McpUnsupportedOAuthError
McpUnsupportedOAuthError
      404: Not found
NotFoundError

    [DELETE] mcp.auth.remove
    Tags: mcp
    Summary: Remove MCP OAuth
    Description: Remove OAuth credentials for an MCP server.
    Parameters:
      - name (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: OAuth credentials removed
Object {
          success *: boolean
        }
      404: Not found
NotFoundError

  ------------------------------------------------------------

  /mcp/{name}/auth/authenticate
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] mcp.auth.authenticate
    Tags: mcp
    Summary: Authenticate MCP OAuth
    Description: Start OAuth flow and wait for callback (opens browser).
    Parameters:
      - name (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: OAuth authentication completed
MCPStatus
      400: McpUnsupportedOAuthError
McpUnsupportedOAuthError
      404: Not found
NotFoundError

  ------------------------------------------------------------

  /mcp/{name}/auth/callback
  ~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] mcp.auth.callback
    Tags: mcp
    Summary: Complete MCP OAuth
    Description: Complete OAuth authentication for a Model Context Protocol (MCP) server using the authorization code.
    Parameters:
      - name (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        code *: string
      }
    Responses:
      200: OAuth authentication completed
MCPStatus
      400: Bad request
BadRequestError
      404: Not found
NotFoundError

  ------------------------------------------------------------

  /mcp/{name}/connect
  ~~~~~~~~~~~~~~~~~~~

    [POST] mcp.connect
    Tags: mcp
    Description: Connect an MCP server.
    Parameters:
      - name (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: MCP server connected successfully
boolean

  ------------------------------------------------------------

  /mcp/{name}/disconnect
  ~~~~~~~~~~~~~~~~~~~~~~

    [POST] mcp.disconnect
    Tags: mcp
    Description: Disconnect an MCP server.
    Parameters:
      - name (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: MCP server disconnected successfully
boolean

  ------------------------------------------------------------

  /path
  ~~~~~

    [GET] path.get
    Tags: instance
    Summary: Get paths
    Description: Retrieve the current working directory and related path information for the OpenCode instance.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Path
Path

  ------------------------------------------------------------

  /permission
  ~~~~~~~~~~~

    [GET] permission.list
    Tags: permission
    Summary: List pending permissions
    Description: Get all pending permission requests across all sessions.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of pending permissions
Array<PermissionRequest>

  ------------------------------------------------------------

  /permission/{requestID}/reply
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] permission.reply
    Tags: permission
    Summary: Respond to permission request
    Description: Approve or deny a permission request from the AI assistant.
    Parameters:
      - requestID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        message: string
        reply *: Enum[once, always, reject]
      }
    Responses:
      200: Permission processed successfully
boolean
      400: Bad request
BadRequestError
      404: Not found
NotFoundError

  ------------------------------------------------------------

  /project
  ~~~~~~~~

    [GET] project.list
    Tags: project
    Summary: List all projects
    Description: Get a list of projects that have been opened with OpenCode.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of projects
Array<Project>

  ------------------------------------------------------------

  /project/current
  ~~~~~~~~~~~~~~~~

    [GET] project.current
    Tags: project
    Summary: Get current project
    Description: Retrieve the currently active project that OpenCode is working with.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Current project information
Project

  ------------------------------------------------------------

  /project/git/init
  ~~~~~~~~~~~~~~~~~

    [POST] project.initGit
    Tags: project
    Summary: Initialize git repository
    Description: Create a git repository for the current project and return the refreshed project info.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Project information after git initialization
Project

  ------------------------------------------------------------

  /project/{projectID}
  ~~~~~~~~~~~~~~~~~~~~

    [PATCH] project.update
    Tags: project
    Summary: Update project
    Description: Update project properties such as name, icon, and commands.
    Parameters:
      - projectID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        commands: Object {
          start: string  # Startup script to run when creating a new workspace (worktree)
        }
        icon: Object {
          color: string
          override: string
          url: string
        }
        name: string
      }
    Responses:
      200: Updated project information
Project
      400: Bad request
BadRequestError
      404: Not found
NotFoundError

  ------------------------------------------------------------

  /provider
  ~~~~~~~~~

    [GET] provider.list
    Tags: provider
    Summary: List providers
    Description: Get a list of all available AI providers, including both available and connected ones.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of providers
Object {
          all *: Array<Provider>
          connected *: Array<string>
          default *: Object{}
        }

  ------------------------------------------------------------

  /provider/auth
  ~~~~~~~~~~~~~~

    [GET] provider.auth
    Tags: provider
    Summary: Get provider auth methods
    Description: Retrieve available authentication methods for all AI providers.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Provider auth methods
Object{}

  ------------------------------------------------------------

  /provider/{providerID}/oauth/authorize
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] provider.oauth.authorize
    Tags: provider
    Summary: Start OAuth authorization
    Description: Start the OAuth authorization flow for a provider.
    Parameters:
      - providerID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        inputs: Object{}
        method *: number  # Auth method index
      }
    Responses:
      200: Authorization URL and method
ProviderAuthAuthorization
      400: ProviderAuthError
ProviderAuthError1

  ------------------------------------------------------------

  /provider/{providerID}/oauth/callback
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] provider.oauth.callback
    Tags: provider
    Summary: Handle OAuth callback
    Description: Handle the OAuth callback from a provider after user authorization.
    Parameters:
      - providerID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        code: string
        method *: number  # Auth method index
      }
    Responses:
      200: OAuth callback processed successfully
boolean
      400: ProviderAuthError
ProviderAuthError1

  ------------------------------------------------------------

  /pty
  ~~~~

    [GET] pty.list
    Tags: pty
    Summary: List PTY sessions
    Description: Get a list of all active pseudo-terminal (PTY) sessions managed by OpenCode.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of sessions
Array<Pty>

    [POST] pty.create
    Tags: pty
    Summary: Create PTY session
    Description: Create a new pseudo-terminal (PTY) session for running shell commands and processes.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        args: Array<string>
        command: string
        cwd: string
        env: Object{}
        title: string
      }
    Responses:
      200: Created session
Pty
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /pty/shells
  ~~~~~~~~~~~

    [GET] pty.shells
    Tags: pty
    Summary: List available shells
    Description: Get a list of available shells on the system.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of shells
Array<Object {
          acceptable *: boolean
          name *: string
          path *: string
        }>

  ------------------------------------------------------------

  /pty/{ptyID}
  ~~~~~~~~~~~~

    [GET] pty.get
    Tags: pty
    Summary: Get PTY session
    Description: Retrieve detailed information about a specific pseudo-terminal (PTY) session.
    Parameters:
      - ptyID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Session info
Pty
      404: NotFoundError
NotFoundError

    [PUT] pty.update
    Tags: pty
    Summary: Update PTY session
    Description: Update properties of an existing pseudo-terminal (PTY) session.
    Parameters:
      - ptyID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        size: Object {
          cols *: integer
          rows *: integer
        }
        title: string
      }
    Responses:
      200: Updated session
Pty
      400: Bad request
BadRequestError

    [DELETE] pty.remove
    Tags: pty
    Summary: Remove PTY session
    Description: Remove and terminate a specific pseudo-terminal (PTY) session.
    Parameters:
      - ptyID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Session removed
boolean
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /pty/{ptyID}/connect
  ~~~~~~~~~~~~~~~~~~~~

    [GET] pty.connect
    Tags: pty
    Summary: Connect to PTY session
    Description: Establish a WebSocket connection to interact with a pseudo-terminal (PTY) session in real-time.
    Parameters:
      - ptyID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Connected session
boolean
      403: Forbidden
effect_HttpApiError_Forbidden
      404: Not found
NotFoundError

  ------------------------------------------------------------

  /pty/{ptyID}/connect-token
  ~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] pty.connectToken
    Tags: pty
    Summary: Create PTY WebSocket token
    Description: Create a short-lived ticket for opening a PTY WebSocket connection.
    Parameters:
      - ptyID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: WebSocket connect token
Object {
          expires_in *: integer
          ticket *: string
        }
      403: Forbidden
effect_HttpApiError_Forbidden
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /question
  ~~~~~~~~~

    [GET] question.list
    Tags: question
    Summary: List pending questions
    Description: Get all pending question requests across all sessions.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of pending questions
Array<QuestionRequest>

  ------------------------------------------------------------

  /question/{requestID}/reject
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] question.reject
    Tags: question
    Summary: Reject question request
    Description: Reject a question request from the AI assistant.
    Parameters:
      - requestID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Question rejected successfully
boolean
      400: Bad request
BadRequestError
      404: Not found
NotFoundError

  ------------------------------------------------------------

  /question/{requestID}/reply
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] question.reply
    Tags: question
    Summary: Reply to question request
    Description: Provide answers to a question request from the AI assistant.
    Parameters:
      - requestID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        answers *: Array<QuestionAnswer>  # User answers in order of questions (each answer is an array of selected labels)
      }
    Responses:
      200: Question answered successfully
boolean
      400: Bad request
BadRequestError
      404: Not found
NotFoundError

  ------------------------------------------------------------

  /session
  ~~~~~~~~

    [GET] session.list
    Tags: session
    Summary: List sessions
    Description: Get a list of all OpenCode sessions, sorted by most recently updated.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - scope (query): string [project]
      - path (query): string
      - roots (query): any
      - start (query): number
      - search (query): string
      - limit (query): number
    Responses:
      200: List of sessions
Array<Session>

    [POST] session.create
    Tags: session
    Summary: Create session
    Description: Create a new OpenCode session for interacting with AI assistants and managing conversations.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        agent: string
        model: Object {
          id *: string
          providerID *: string
          variant: string
        }
        parentID: string
        permission: PermissionRuleset
        title: string
        workspaceID: string
      }
    Responses:
      200: Successfully created session
Session
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /session/status
  ~~~~~~~~~~~~~~~

    [GET] session.status
    Tags: session
    Summary: Get session status
    Description: Retrieve the current status of all sessions, including active, idle, and completed states.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Get session status
Object{}
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /session/{sessionID}
  ~~~~~~~~~~~~~~~~~~~~

    [GET] session.get
    Tags: session
    Summary: Get session
    Description: Retrieve detailed information about a specific OpenCode session.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Get session
Session
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

    [DELETE] session.delete
    Tags: session
    Summary: Delete session
    Description: Delete a session and permanently remove all associated data, including messages and history.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Successfully deleted session
boolean
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

    [PATCH] session.update
    Tags: session
    Summary: Update session
    Description: Update properties of an existing session, such as title or other metadata.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        permission: PermissionRuleset
        time: Object {
          archived: number
        }
        title: string
      }
    Responses:
      200: Successfully updated session
Session
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/abort
  ~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.abort
    Tags: session
    Summary: Abort session
    Description: Abort an active session and stop any ongoing AI processing or command execution.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Aborted session
boolean
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /session/{sessionID}/children
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] session.children
    Tags: session
    Summary: Get session children
    Description: Retrieve all child sessions that were forked from the specified parent session.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of children
Array<Session>
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/command
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.command
    Tags: session
    Summary: Send command
    Description: Send a new command to a session for execution by the AI assistant.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        agent: string
        arguments *: string
        command *: string
        messageID: string
        model: string
        parts: Array<Object {
          filename: string
          id: string
          mime *: string
          source: FilePartSource
          type *: Enum[file]
          url *: string
        }>
        variant: string
      }
    Responses:
      200: Created message
Object {
          info *: AssistantMessage
          parts *: Array<Part>
        }
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/diff
  ~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] session.diff
    Tags: session
    Summary: Get message diff
    Description: Get the file changes (diff) that resulted from a specific user message in the session.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
      - messageID (query): string
    Responses:
      200: Successfully retrieved diff
Array<SnapshotFileDiff>

  ------------------------------------------------------------

  /session/{sessionID}/fork
  ~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.fork
    Tags: session
    Summary: Fork session
    Description: Create a new session by forking an existing session at a specific message point.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        messageID: string
      }
    Responses:
      200: 200
Session
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/init
  ~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.init
    Tags: session
    Summary: Initialize session
    Description: Analyze the current application and create an AGENTS.md file with project-specific agent configurations.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        messageID *: string
        modelID *: string
        providerID *: string
      }
    Responses:
      200: 200
boolean
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/message
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] session.messages
    Tags: session
    Summary: Get session messages
    Description: Retrieve all messages in a session, including user prompts and AI responses.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
      - limit (query): integer
      - before (query): string
    Responses:
      200: List of messages
Array<Object {
          info *: Message
          parts *: Array<Part>
        }>
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

    [POST] session.prompt
    Tags: session
    Summary: Send message
    Description: Create and send a new message to a session, streaming the AI response.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        agent: string
        format: OutputFormat
        messageID: string
        model: Object {
          modelID *: string
          providerID *: string
        }
        noReply: boolean
        parts *: Array<Union(TextPartInput, FilePartInput, AgentPartInput, SubtaskPartInput)>
        system: string
        tools: Object{}
        variant: string
      }
    Responses:
      200: Created message
Object {
          info *: AssistantMessage
          parts *: Array<Part>
        }
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/message/{messageID}
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] session.message
    Tags: session
    Summary: Get message
    Description: Retrieve a specific message from a session by its message ID.
    Parameters:
      - sessionID (path): string (required)
      - messageID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Message
Object {
          info *: Message
          parts *: Array<Part>
        }
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

    [DELETE] session.deleteMessage
    Tags: session
    Summary: Delete message
    Description: Permanently delete a specific message and all of its parts from a session without reverting file changes.
    Parameters:
      - sessionID (path): string (required)
      - messageID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Successfully deleted message
boolean
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/message/{messageID}/part/{partID}
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [DELETE] part.delete
    Tags: session
    Description: Delete a part from a message.
    Parameters:
      - sessionID (path): string (required)
      - messageID (path): string (required)
      - partID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Successfully deleted part
boolean
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

    [PATCH] part.update
    Tags: session
    Description: Update a part in a message.
    Parameters:
      - sessionID (path): string (required)
      - messageID (path): string (required)
      - partID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Part
    Responses:
      200: Successfully updated part
Part
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/permissions/{permissionID}
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] permission.respond
    Tags: session
    Summary: Respond to permission
    Description: Approve or deny a permission request from the AI assistant.
    Parameters:
      - sessionID (path): string (required)
      - permissionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        response *: Enum[once, always, reject]
      }
    Responses:
      200: Permission processed successfully
boolean
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/prompt_async
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.prompt_async
    Tags: session
    Summary: Send async message
    Description: Create and send a new message to a session asynchronously, starting the session if needed and returning immediately.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        agent: string
        format: OutputFormat
        messageID: string
        model: Object {
          modelID *: string
          providerID *: string
        }
        noReply: boolean
        parts *: Array<Union(TextPartInput, FilePartInput, AgentPartInput, SubtaskPartInput)>
        system: string
        tools: Object{}
        variant: string
      }
    Responses:
      204: Prompt accepted
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/revert
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.revert
    Tags: session
    Summary: Revert message
    Description: Revert a specific message in a session, undoing its effects and restoring the previous state.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        messageID *: string
        partID: string
      }
    Responses:
      200: Updated session
Session
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/share
  ~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.share
    Tags: session
    Summary: Share session
    Description: Create a shareable link for a session, allowing others to view the conversation.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Successfully shared session
Session
      404: NotFoundError
NotFoundError
      500: InternalServerError
effect_HttpApiError_InternalServerError

    [DELETE] session.unshare
    Tags: session
    Summary: Unshare session
    Description: Remove the shareable link for a session, making it private again.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Successfully unshared session
Session
      404: NotFoundError
NotFoundError
      500: InternalServerError
effect_HttpApiError_InternalServerError

  ------------------------------------------------------------

  /session/{sessionID}/shell
  ~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.shell
    Tags: session
    Summary: Run shell command
    Description: Execute a shell command within the session context and return the AI's response.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        agent *: string
        command *: string
        messageID: string
        model: Object {
          modelID *: string
          providerID *: string
        }
      }
    Responses:
      200: Created message
Object {
          info *: Message
          parts *: Array<Part>
        }
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/summarize
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.summarize
    Tags: session
    Summary: Summarize session
    Description: Generate a concise summary of the session using AI compaction to preserve key information.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        auto: boolean
        modelID *: string
        providerID *: string
      }
    Responses:
      200: Summarized session
boolean
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/todo
  ~~~~~~~~~~~~~~~~~~~~~~~~~

    [GET] session.todo
    Tags: session
    Summary: Get session todos
    Description: Retrieve the todo list associated with a specific session, showing tasks and action items.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Todo list
Array<Todo>
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /session/{sessionID}/unrevert
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    [POST] session.unrevert
    Tags: session
    Summary: Restore reverted messages
    Description: Restore all previously reverted messages in a session.
    Parameters:
      - sessionID (path): string (required)
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Updated session
Session
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /skill
  ~~~~~~

    [GET] app.skills
    Tags: instance
    Summary: List skills
    Description: Get a list of all available skills in the OpenCode system.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: List of skills
Array<Object {
          content *: string
          description: string
          location *: string
          name *: string
        }>

  ------------------------------------------------------------

  /sync/history
  ~~~~~~~~~~~~~

    [POST] sync.history.list
    Tags: sync
    Summary: List sync events
    Description: List sync events for all aggregates. Keys are aggregate IDs the client already knows about, values are the last known sequence ID. Events with seq > value are returned for those aggregates. Aggregates not listed in the input get their full history.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object{}
    Responses:
      200: Sync events
Array<Object {
          aggregate_id *: string
          data *: Object{}
          id *: string
          seq *: integer
          type *: string
        }>
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /sync/replay
  ~~~~~~~~~~~~

    [POST] sync.replay
    Tags: sync
    Summary: Replay sync events
    Description: Validate and replay a complete sync event history.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        directory *: string
        events *: Array<Object {
          aggregateID *: string
          data *: Object{}
          id *: string
          seq *: integer
          type *: string
        }>
      }
    Responses:
      200: Replayed sync events
Object {
          sessionID *: string
        }
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /sync/start
  ~~~~~~~~~~~

    [POST] sync.start
    Tags: sync
    Summary: Start workspace sync
    Description: Start sync loops for workspaces in the current project that have active sessions.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Workspace sync started
boolean

  ------------------------------------------------------------

  /sync/steal
  ~~~~~~~~~~~

    [POST] sync.steal
    Tags: sync
    Summary: Steal session into workspace
    Description: Update a session to belong to the current workspace through the sync event system.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        sessionID *: string
      }
    Responses:
      200: Session stolen into workspace
Object {
          sessionID *: string
        }
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /tui/append-prompt
  ~~~~~~~~~~~~~~~~~~

    [POST] tui.appendPrompt
    Tags: tui
    Summary: Append TUI prompt
    Description: Append prompt to the TUI.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        text *: string
      }
    Responses:
      200: Prompt processed successfully
boolean
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /tui/clear-prompt
  ~~~~~~~~~~~~~~~~~

    [POST] tui.clearPrompt
    Tags: tui
    Summary: Clear TUI prompt
    Description: Clear the prompt.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Prompt cleared successfully
boolean

  ------------------------------------------------------------

  /tui/control/next
  ~~~~~~~~~~~~~~~~~

    [GET] tui.control.next
    Tags: tui
    Summary: Get next TUI request
    Description: Retrieve the next TUI request from the queue for processing.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Next TUI request
Object {
          body *: Object{}
          path *: string
        }

  ------------------------------------------------------------

  /tui/control/response
  ~~~~~~~~~~~~~~~~~~~~~

    [POST] tui.control.response
    Tags: tui
    Summary: Submit TUI response
    Description: Submit a response to the TUI request queue to complete a pending request.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object{}
    Responses:
      200: Response submitted successfully
boolean

  ------------------------------------------------------------

  /tui/execute-command
  ~~~~~~~~~~~~~~~~~~~~

    [POST] tui.executeCommand
    Tags: tui
    Summary: Execute TUI command
    Description: Execute a TUI command.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        command *: string
      }
    Responses:
      200: Command executed successfully
boolean
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /tui/open-help
  ~~~~~~~~~~~~~~

    [POST] tui.openHelp
    Tags: tui
    Summary: Open help dialog
    Description: Open the help dialog in the TUI to display user assistance information.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Help dialog opened successfully
boolean

  ------------------------------------------------------------

  /tui/open-models
  ~~~~~~~~~~~~~~~~

    [POST] tui.openModels
    Tags: tui
    Summary: Open models dialog
    Description: Open the model dialog.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Model dialog opened successfully
boolean

  ------------------------------------------------------------

  /tui/open-sessions
  ~~~~~~~~~~~~~~~~~~

    [POST] tui.openSessions
    Tags: tui
    Summary: Open sessions dialog
    Description: Open the session dialog.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Session dialog opened successfully
boolean

  ------------------------------------------------------------

  /tui/open-themes
  ~~~~~~~~~~~~~~~~

    [POST] tui.openThemes
    Tags: tui
    Summary: Open themes dialog
    Description: Open the theme dialog.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Theme dialog opened successfully
boolean

  ------------------------------------------------------------

  /tui/publish
  ~~~~~~~~~~~~

    [POST] tui.publish
    Tags: tui
    Summary: Publish TUI event
    Description: Publish a TUI event.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Union(EventTuiPromptAppend, EventTuiCommandExecute, EventTuiToastShow, EventTuiSessionSelect)
    Responses:
      200: Event published successfully
boolean
      400: Bad request
BadRequestError

  ------------------------------------------------------------

  /tui/select-session
  ~~~~~~~~~~~~~~~~~~~

    [POST] tui.selectSession
    Tags: tui
    Summary: Select session
    Description: Navigate the TUI to display the specified session.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        sessionID *: string  # Session ID to navigate to
      }
    Responses:
      200: Session selected successfully
boolean
      400: Bad request
BadRequestError
      404: NotFoundError
NotFoundError

  ------------------------------------------------------------

  /tui/show-toast
  ~~~~~~~~~~~~~~~

    [POST] tui.showToast
    Tags: tui
    Summary: Show TUI toast
    Description: Show a toast notification in the TUI.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        duration: integer
        message *: string
        title: string
        variant *: Enum[info, success, warning, error]
      }
    Responses:
      200: Toast notification shown successfully
boolean

  ------------------------------------------------------------

  /tui/submit-prompt
  ~~~~~~~~~~~~~~~~~~

    [POST] tui.submitPrompt
    Tags: tui
    Summary: Submit TUI prompt
    Description: Submit the prompt.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Prompt submitted successfully
boolean

  ------------------------------------------------------------

  /vcs
  ~~~~

    [GET] vcs.get
    Tags: instance
    Summary: Get VCS info
    Description: Retrieve version control system (VCS) information for the current project, such as git branch.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: VCS info
VcsInfo

  ------------------------------------------------------------

  /vcs/apply
  ~~~~~~~~~~

    [POST] vcs.apply
    Tags: instance
    Summary: Apply VCS patch
    Description: Apply a raw patch to the current working tree.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Request Body:
Object {
        patch *: string
      }
    Responses:
      200: VCS patch applied
Object {
          applied *: boolean
        }
      400: VcsApplyError
VcsApplyError

  ------------------------------------------------------------

  /vcs/diff
  ~~~~~~~~~

    [GET] vcs.diff
    Tags: instance
    Summary: Get VCS diff
    Description: Retrieve the current git diff for the working tree or against the default branch.
    Parameters:
      - directory (query): string
      - workspace (query): string
      - mode (query): string (required) [git, branch]
    Responses:
      200: VCS diff
Array<VcsFileDiff>

  ------------------------------------------------------------

  /vcs/diff/raw
  ~~~~~~~~~~~~~

    [GET] vcs.diff.raw
    Tags: instance
    Summary: Get raw VCS diff
    Description: Retrieve a raw patch for current uncommitted changes.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: Raw VCS diff

  ------------------------------------------------------------

  /vcs/status
  ~~~~~~~~~~~

    [GET] vcs.status
    Tags: instance
    Summary: Get VCS status
    Description: Retrieve changed files in the current working tree without patches.
    Parameters:
      - directory (query): string
      - workspace (query): string
    Responses:
      200: VCS status
Array<VcsFileStatus>

  ------------------------------------------------------------


SCHEMAS
================================================================================

  APIError
  ~~~~~~~~
Object {
      data *: Object {
        isRetryable *: boolean
        message *: string
        metadata: Object{}
        responseBody: string
        responseHeaders: Object{}
        statusCode: integer
      }
      name *: Enum[APIError]
    }


  Agent
  ~~~~~
Object {
      color: string
      description: string
      hidden: boolean
      mode *: Enum[subagent, primary, all]
      model: Object {
        modelID *: string
        providerID *: string
      }
      name *: string
      native: boolean
      options *: Object{}
      permission *: PermissionRuleset
      prompt: string
      steps: number
      temperature: number
      topP: number
      variant: string
    }


  AgentConfig
  ~~~~~~~~~~~
Object {
      color: Union(string, Enum[primary, secondary, accent, success, warning, error, info])  # Hex color code (e.g., #FF5733) or theme color (e.g., primary)
      description: string
      disable: boolean
      hidden: boolean
      maxSteps: integer
      mode: Enum[subagent, primary, all]
      model: string
      options: Object{}
      permission: PermissionConfig
      prompt: string
      steps: integer
      temperature: number
      tools: Object{}
      top_p: number
      variant: string
    }


  AgentPart
  ~~~~~~~~~
Object {
      id *: string
      messageID *: string
      name *: string
      sessionID *: string
      source: Object {
        end *: integer
        start *: integer
        value *: string
      }
      type *: Enum[agent]
    }


  AgentPartInput
  ~~~~~~~~~~~~~~
Object {
      id: string
      name *: string
      source: Object {
        end *: integer
        start *: integer
        value *: string
      }
      type *: Enum[agent]
    }


  ApiAuth
  ~~~~~~~
Object {
      key *: string
      metadata: Object{}
      type *: Enum[api]
    }


  AssistantMessage
  ~~~~~~~~~~~~~~~~
Object {
      agent *: string
      cost *: number
      error: Union(ProviderAuthError, UnknownError, MessageOutputLengthError, MessageAbortedError, StructuredOutputError) ...(+2 more)
      finish: string
      id *: string
      mode *: string
      modelID *: string
      parentID *: string
      path *: Object {
        cwd *: string
        root *: string
      }
      providerID *: string
      role *: Enum[assistant]
      sessionID *: string
      structured: Object{}
      summary: boolean
      time *: Object {
        completed: integer
        created *: integer
      }
      tokens *: Object {
        cache *: Object {
          read *: number
          write *: number
        }
        input *: number
        output *: number
        reasoning *: number
        total: number
      }
      variant: string
    }


  AttachmentConfig
  ~~~~~~~~~~~~~~~~
Object {
      image: ImageAttachmentConfig
    }


  Auth
  ~~~~
Union(OAuth, ApiAuth, WellKnownAuth)


  BadRequestError
  ~~~~~~~~~~~~~~~
Object {
      data *: Object {
        kind: Enum[Params, Headers, Query, Body, Payload]
        message *: string
      }
      name *: Enum[BadRequest]
    }


  Command
  ~~~~~~~
Object {
      agent: string
      description: string
      hints *: Array<string>
      model: string
      name *: string
      source: Enum[command, mcp, skill]
      subtask: boolean
      template *: string
    }


  CompactionPart
  ~~~~~~~~~~~~~~
Object {
      auto *: boolean
      id *: string
      messageID *: string
      overflow: boolean
      sessionID *: string
      tail_start_id: string
      type *: Enum[compaction]
    }


  Config
  ~~~~~~
Object {
      $schema: string
      agent: Object {
        build: AgentConfig
        compaction: AgentConfig
        explore: AgentConfig
        general: AgentConfig
        plan: AgentConfig
        scout: AgentConfig
        summary: AgentConfig
        title: AgentConfig
      }
      attachment: AttachmentConfig
      autoshare: boolean
      autoupdate: Union(boolean, Enum[notify])  # Automatically update to the latest version. Set to true to auto-update, false to disable, or 'notify' to show update notifications
      command: Object{}
      compaction: Object {
        auto: boolean
        preserve_recent_tokens: integer
        prune: boolean
        reserved: integer
        tail_turns: integer
      }
      default_agent: string
      disabled_providers: Array<string>
      enabled_providers: Array<string>
      enterprise: Object {
        url: string
      }
      experimental: Object {
        batch_tool: boolean
        continue_loop_on_deny: boolean
        disable_paste_summary: boolean
        mcp_timeout: integer
        openTelemetry: boolean
        primary_tools: Array<string>
      }
      formatter: Union(boolean, Object{})  # Enable or configure formatters. Omit or set to false to disable, true to enable built-ins, or an object to enable built-ins with overrides.
      instructions: Array<string>
      layout: LayoutConfig
      logLevel: LogLevel
      lsp: Union(boolean, Object{})  # Enable or configure LSP servers. Omit or set to false to disable, true to enable built-ins, or an object to enable built-ins with overrides.
      mcp: Object{}
      mode: Object {
        build: AgentConfig
        plan: AgentConfig
      }
      model: string
      permission: PermissionConfig
      plugin: Array<Union(string, Array<Object{}>)>
      provider: Object{}
      reference: ReferenceConfig
      server: ServerConfig
      share: Enum[manual, auto, disabled]
      shell: string
      skills: Object {
        paths: Array<string>
        urls: Array<string>
      }
      small_model: string
      snapshot: boolean
      tool_output: Object {
        max_bytes: integer
        max_lines: integer
      }
      tools: Object{}
      username: string
      watcher: Object {
        ignore: Array<string>
      }
    }


  ConsoleState
  ~~~~~~~~~~~~
Object {
      activeOrgName: string
      consoleManagedProviders *: Array<string>
      switchableOrgCount *: integer
    }


  ContextOverflowError
  ~~~~~~~~~~~~~~~~~~~~
Object {
      data *: Object {
        message *: string
        responseBody: string
      }
      name *: Enum[ContextOverflowError]
    }


  Event
  ~~~~~
Union(EventServerInstanceDisposed, EventPermissionAsked, EventPermissionReplied, EventLspClientDiagnostics, EventLspUpdated) ...(+94 more)


  Event.tui.command.execute
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        command *: Union(Enum[session.list, session.new, session.share, session.interrupt, session.compact, session.page.up, session.page.down, session.line.up, session.line.down, session.half.page.up, session.half.page.down, session.first, session.last, prompt.clear, prompt.submit, agent.cycle], string)
      }
      type *: Enum[tui.command.execute]
    }


  Event.tui.prompt.append
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        text *: string
      }
      type *: Enum[tui.prompt.append]
    }


  Event.tui.session.select
  ~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        sessionID *: string  # Session ID to navigate to
      }
      type *: Enum[tui.session.select]
    }


  Event.tui.toast.show
  ~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        duration: integer
        message *: string
        title: string
        variant *: Enum[info, success, warning, error]
      }
      type *: Enum[tui.toast.show]
    }


  EventCatalogModelUpdated
  ~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        model *: ModelV2Info
      }
      type *: Enum[catalog.model.updated]
    }


  EventCommandExecuted
  ~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        arguments *: string
        messageID *: string
        name *: string
        sessionID *: string
      }
      type *: Enum[command.executed]
    }


  EventFileEdited
  ~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        file *: string
      }
      type *: Enum[file.edited]
    }


  EventFileWatcherUpdated
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        event *: Enum[add, change, unlink]
        file *: string
      }
      type *: Enum[file.watcher.updated]
    }


  EventGlobalDisposed
  ~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object{}
      type *: Enum[global.disposed]
    }


  EventInstallationUpdate-available
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        version *: string
      }
      type *: Enum[installation.update-available]
    }


  EventInstallationUpdated
  ~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        version *: string
      }
      type *: Enum[installation.updated]
    }


  EventLspClientDiagnostics
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        path *: string
        serverID *: string
      }
      type *: Enum[lsp.client.diagnostics]
    }


  EventLspUpdated
  ~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object{}
      type *: Enum[lsp.updated]
    }


  EventMcpBrowserOpenFailed
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        mcpName *: string
        url *: string
      }
      type *: Enum[mcp.browser.open.failed]
    }


  EventMcpToolsChanged
  ~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        server *: string
      }
      type *: Enum[mcp.tools.changed]
    }


  EventMessagePartDelta
  ~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        delta *: string
        field *: string
        messageID *: string
        partID *: string
        sessionID *: string
      }
      type *: Enum[message.part.delta]
    }


  EventMessagePartRemoved
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        messageID *: string
        partID *: string
        sessionID *: string
      }
      type *: Enum[message.part.removed]
    }


  EventMessagePartUpdated
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        part *: Part
        sessionID *: string
        time *: integer
      }
      type *: Enum[message.part.updated]
    }


  EventMessageRemoved
  ~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        messageID *: string
        sessionID *: string
      }
      type *: Enum[message.removed]
    }


  EventMessageUpdated
  ~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        info *: Message
        sessionID *: string
      }
      type *: Enum[message.updated]
    }


  EventPermissionAsked
  ~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: PermissionRequest
      type *: Enum[permission.asked]
    }


  EventPermissionReplied
  ~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        reply *: Enum[once, always, reject]
        requestID *: string
        sessionID *: string
      }
      type *: Enum[permission.replied]
    }


  EventProjectUpdated
  ~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Project
      type *: Enum[project.updated]
    }


  EventPtyCreated
  ~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        info *: Pty
      }
      type *: Enum[pty.created]
    }


  EventPtyDeleted
  ~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        id *: string
      }
      type *: Enum[pty.deleted]
    }


  EventPtyExited
  ~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        exitCode *: integer
        id *: string
      }
      type *: Enum[pty.exited]
    }


  EventPtyUpdated
  ~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        info *: Pty
      }
      type *: Enum[pty.updated]
    }


  EventQuestionAsked
  ~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: QuestionRequest
      type *: Enum[question.asked]
    }


  EventQuestionRejected
  ~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: QuestionRejected
      type *: Enum[question.rejected]
    }


  EventQuestionReplied
  ~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: QuestionReplied
      type *: Enum[question.replied]
    }


  EventServerConnected
  ~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object{}
      type *: Enum[server.connected]
    }


  EventServerInstanceDisposed
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        directory *: string
      }
      type *: Enum[server.instance.disposed]
    }


  EventSessionCompacted
  ~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        sessionID *: string
      }
      type *: Enum[session.compacted]
    }


  EventSessionCreated
  ~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        info *: Session
        sessionID *: string
      }
      type *: Enum[session.created]
    }


  EventSessionDeleted
  ~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        info *: Session
        sessionID *: string
      }
      type *: Enum[session.deleted]
    }


  EventSessionDiff
  ~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        diff *: Array<SnapshotFileDiff>
        sessionID *: string
      }
      type *: Enum[session.diff]
    }


  EventSessionError
  ~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        error: Union(ProviderAuthError, UnknownError, MessageOutputLengthError, MessageAbortedError, StructuredOutputError) ...(+2 more)
        sessionID: string
      }
      type *: Enum[session.error]
    }


  EventSessionIdle
  ~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        sessionID *: string
      }
      type *: Enum[session.idle]
    }


  EventSessionNextAgentSwitched
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        agent *: string
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.agent.switched]
    }


  EventSessionNextCompactionDelta
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        sessionID *: string
        text *: string
        timestamp *: number
      }
      type *: Enum[session.next.compaction.delta]
    }


  EventSessionNextCompactionEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        include: string
        sessionID *: string
        text *: string
        timestamp *: number
      }
      type *: Enum[session.next.compaction.ended]
    }


  EventSessionNextCompactionStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        reason *: Enum[auto, manual]
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.compaction.started]
    }


  EventSessionNextModelSwitched
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        model *: Object {
          id *: string
          providerID *: string
          variant *: string
        }
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.model.switched]
    }


  EventSessionNextPrompted
  ~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        prompt *: Prompt
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.prompted]
    }


  EventSessionNextReasoningDelta
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        delta *: string
        reasoningID *: string
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.reasoning.delta]
    }


  EventSessionNextReasoningEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        reasoningID *: string
        sessionID *: string
        text *: string
        timestamp *: number
      }
      type *: Enum[session.next.reasoning.ended]
    }


  EventSessionNextReasoningStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        reasoningID *: string
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.reasoning.started]
    }


  EventSessionNextRetried
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        attempt *: number
        error *: SessionNextRetry_error
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.retried]
    }


  EventSessionNextShellEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        callID *: string
        output *: string
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.shell.ended]
    }


  EventSessionNextShellStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        callID *: string
        command *: string
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.shell.started]
    }


  EventSessionNextStepEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        cost *: number
        finish *: string
        sessionID *: string
        snapshot: string
        timestamp *: number
        tokens *: Object {
          cache *: Object {
            read *: number
            write *: number
          }
          input *: number
          output *: number
          reasoning *: number
        }
      }
      type *: Enum[session.next.step.ended]
    }


  EventSessionNextStepFailed
  ~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        error *: SessionErrorUnknown
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.step.failed]
    }


  EventSessionNextStepStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        agent *: string
        model *: Object {
          id *: string
          providerID *: string
          variant *: string
        }
        sessionID *: string
        snapshot: string
        timestamp *: number
      }
      type *: Enum[session.next.step.started]
    }


  EventSessionNextSynthetic
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        sessionID *: string
        text *: string
        timestamp *: number
      }
      type *: Enum[session.next.synthetic]
    }


  EventSessionNextTextDelta
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        delta *: string
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.text.delta]
    }


  EventSessionNextTextEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        sessionID *: string
        text *: string
        timestamp *: number
      }
      type *: Enum[session.next.text.ended]
    }


  EventSessionNextTextStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.text.started]
    }


  EventSessionNextToolCalled
  ~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        callID *: string
        input *: Object{}
        provider *: Object {
          executed *: boolean
          metadata: Object{}
        }
        sessionID *: string
        timestamp *: number
        tool *: string
      }
      type *: Enum[session.next.tool.called]
    }


  EventSessionNextToolFailed
  ~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        callID *: string
        error *: SessionErrorUnknown
        provider *: Object {
          executed *: boolean
          metadata: Object{}
        }
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.tool.failed]
    }


  EventSessionNextToolInputDelta
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        callID *: string
        delta *: string
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.tool.input.delta]
    }


  EventSessionNextToolInputEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        callID *: string
        sessionID *: string
        text *: string
        timestamp *: number
      }
      type *: Enum[session.next.tool.input.ended]
    }


  EventSessionNextToolInputStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        callID *: string
        name *: string
        sessionID *: string
        timestamp *: number
      }
      type *: Enum[session.next.tool.input.started]
    }


  EventSessionNextToolProgress
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        callID *: string
        content *: Array<Union(ToolTextContent, ToolFileContent)>
        sessionID *: string
        structured *: Object{}
        timestamp *: number
      }
      type *: Enum[session.next.tool.progress]
    }


  EventSessionNextToolSuccess
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        callID *: string
        content *: Array<Union(ToolTextContent, ToolFileContent)>
        provider *: Object {
          executed *: boolean
          metadata: Object{}
        }
        sessionID *: string
        structured *: Object{}
        timestamp *: number
      }
      type *: Enum[session.next.tool.success]
    }


  EventSessionStatus
  ~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        sessionID *: string
        status *: SessionStatus
      }
      type *: Enum[session.status]
    }


  EventSessionUpdated
  ~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        info *: Session
        sessionID *: string
      }
      type *: Enum[session.updated]
    }


  EventTodoUpdated
  ~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        sessionID *: string
        todos *: Array<Todo>
      }
      type *: Enum[todo.updated]
    }


  EventTuiCommandExecute
  ~~~~~~~~~~~~~~~~~~~~~~
Object {
      properties *: Object {
        command *: Union(Enum[session.list, session.new, session.share, session.interrupt, session.compact, session.page.up, session.page.down, session.line.up, session.line.down, session.half.page.up, session.half.page.down, session.first, session.last, prompt.clear, prompt.submit, agent.cycle], string)
      }
      type *: Enum[tui.command.execute]
    }


  EventTuiPromptAppend
  ~~~~~~~~~~~~~~~~~~~~
Object {
      properties *: Object {
        text *: string
      }
      type *: Enum[tui.prompt.append]
    }


  EventTuiSessionSelect
  ~~~~~~~~~~~~~~~~~~~~~
Object {
      properties *: Object {
        sessionID *: string  # Session ID to navigate to
      }
      type *: Enum[tui.session.select]
    }


  EventTuiToastShow
  ~~~~~~~~~~~~~~~~~
Object {
      properties *: Object {
        duration: integer
        message *: string
        title: string
        variant *: Enum[info, success, warning, error]
      }
      type *: Enum[tui.toast.show]
    }


  EventTuiToastShow1
  ~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        duration: integer
        message *: string
        title: string
        variant *: Enum[info, success, warning, error]
      }
      type *: Enum[tui.toast.show]
    }


  EventVcsBranchUpdated
  ~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        branch: string
      }
      type *: Enum[vcs.branch.updated]
    }


  EventWorkspaceFailed
  ~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        message *: string
      }
      type *: Enum[workspace.failed]
    }


  EventWorkspaceReady
  ~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        name *: string
      }
      type *: Enum[workspace.ready]
    }


  EventWorkspaceStatus
  ~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        status *: Enum[connected, connecting, disconnected, error]
        workspaceID *: string
      }
      type *: Enum[workspace.status]
    }


  EventWorktreeFailed
  ~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        message *: string
      }
      type *: Enum[worktree.failed]
    }


  EventWorktreeReady
  ~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      properties *: Object {
        branch: string
        name *: string
      }
      type *: Enum[worktree.ready]
    }


  File
  ~~~~
Object {
      added *: integer
      path *: string
      removed *: integer
      status *: Enum[added, deleted, modified]
    }


  FileContent
  ~~~~~~~~~~~
Object {
      content *: string
      diff: string
      encoding: Enum[base64]
      mimeType: string
      patch: Object {
        hunks *: Array<Object {
          lines *: Array<string>
          newLines *: integer
          newStart *: integer
          oldLines *: integer
          oldStart *: integer
        }>
        index: string
        newFileName *: string
        newHeader: string
        oldFileName *: string
        oldHeader: string
      }
      type *: Enum[text, binary]
    }


  FileNode
  ~~~~~~~~
Object {
      absolute *: string
      ignored *: boolean
      name *: string
      path *: string
      type *: Enum[file, directory]
    }


  FilePart
  ~~~~~~~~
Object {
      filename: string
      id *: string
      messageID *: string
      mime *: string
      sessionID *: string
      source: FilePartSource
      type *: Enum[file]
      url *: string
    }


  FilePartInput
  ~~~~~~~~~~~~~
Object {
      filename: string
      id: string
      mime *: string
      source: FilePartSource
      type *: Enum[file]
      url *: string
    }


  FilePartSource
  ~~~~~~~~~~~~~~
Union(FileSource, SymbolSource, ResourceSource)


  FilePartSourceText
  ~~~~~~~~~~~~~~~~~~
Object {
      end *: number
      start *: number
      value *: string
    }


  FileSource
  ~~~~~~~~~~
Object {
      path *: string
      text *: FilePartSourceText
      type *: Enum[file]
    }


  FormatterStatus
  ~~~~~~~~~~~~~~~
Object {
      enabled *: boolean
      extensions *: Array<string>
      name *: string
    }


  GlobalEvent
  ~~~~~~~~~~~
Object {
      directory *: string
      payload *: Union(EventServerInstanceDisposed, EventPermissionAsked, EventPermissionReplied, EventLspClientDiagnostics, EventLspUpdated) ...(+127 more)
      project: string
      workspace: string
    }


  GlobalSession
  ~~~~~~~~~~~~~
Object {
      agent: string
      cost: number
      directory *: string
      id *: string
      model: Object {
        id *: string
        providerID *: string
        variant: string
      }
      parentID: string
      path: string
      permission: PermissionRuleset
      project *: Union(ProjectSummary, null)
      projectID *: string
      revert: Object {
        diff: string
        messageID *: string
        partID: string
        snapshot: string
      }
      share: Object {
        url *: string
      }
      slug *: string
      summary: Object {
        additions *: number
        deletions *: number
        diffs: Array<SnapshotFileDiff>
        files *: number
      }
      time *: Object {
        archived: number
        compacting: integer
        created *: integer
        updated *: integer
      }
      title *: string
      tokens: Object {
        cache *: Object {
          read *: number
          write *: number
        }
        input *: number
        output *: number
        reasoning *: number
      }
      version *: string
      workspaceID: string
    }


  ImageAttachmentConfig
  ~~~~~~~~~~~~~~~~~~~~~
Object {
      auto_resize: boolean
      max_base64_bytes: integer
      max_height: integer
      max_width: integer
    }


  JSONSchema
  ~~~~~~~~~~
Object{}


  LSPStatus
  ~~~~~~~~~
Object {
      id *: string
      name *: string
      root *: string
      status *: Enum[connected, error]
    }


  LayoutConfig
  ~~~~~~~~~~~~
    @deprecated Always uses stretch layout.
Enum[auto, stretch]


  LogLevel
  ~~~~~~~~
    Log level
Enum[DEBUG, INFO, WARN, ERROR]


  MCPStatus
  ~~~~~~~~~
Union(MCPStatusConnected, MCPStatusDisabled, MCPStatusFailed, MCPStatusNeedsAuth, MCPStatusNeedsClientRegistration)


  MCPStatusConnected
  ~~~~~~~~~~~~~~~~~~
Object {
      status *: Enum[connected]
    }


  MCPStatusDisabled
  ~~~~~~~~~~~~~~~~~
Object {
      status *: Enum[disabled]
    }


  MCPStatusFailed
  ~~~~~~~~~~~~~~~
Object {
      error *: string
      status *: Enum[failed]
    }


  MCPStatusNeedsAuth
  ~~~~~~~~~~~~~~~~~~
Object {
      status *: Enum[needs_auth]
    }


  MCPStatusNeedsClientRegistration
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      error *: string
      status *: Enum[needs_client_registration]
    }


  McpLocalConfig
  ~~~~~~~~~~~~~~
Object {
      command *: Array<string>  # Command and arguments to run the MCP server
      enabled: boolean
      environment: Object{}
      timeout: integer
      type *: Enum[local]  # Type of MCP server connection
    }


  McpOAuthConfig
  ~~~~~~~~~~~~~~
Object {
      clientId: string
      clientSecret: string
      redirectUri: string
      scope: string
    }


  McpRemoteConfig
  ~~~~~~~~~~~~~~~
Object {
      enabled: boolean
      headers: Object{}
      oauth: Union(McpOAuthConfig, boolean)  # OAuth authentication configuration for the MCP server. Set to false to disable OAuth auto-detection.
      timeout: integer
      type *: Enum[remote]  # Type of MCP server connection
      url *: string  # URL of the remote MCP server
    }


  McpResource
  ~~~~~~~~~~~
Object {
      client *: string
      description: string
      mimeType: string
      name *: string
      uri *: string
    }


  McpUnsupportedOAuthError
  ~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      error *: string
    }


  Message
  ~~~~~~~
Union(UserMessage, AssistantMessage)


  MessageAbortedError
  ~~~~~~~~~~~~~~~~~~~
Object {
      data *: Object {
        message *: string
      }
      name *: Enum[MessageAbortedError]
    }


  MessageOutputLengthError
  ~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      data *: Object{}
      name *: Enum[MessageOutputLengthError]
    }


  Model
  ~~~~~
Object {
      api *: Object {
        id *: string
        npm *: string
        url *: string
      }
      capabilities *: Object {
        attachment *: boolean
        input *: Object {
          audio *: boolean
          image *: boolean
          pdf *: boolean
          text *: boolean
          video *: boolean
        }
        interleaved *: Union(boolean, Object {
          field *: Enum[reasoning_content, reasoning_details]
        })
        output *: Object {
          audio *: boolean
          image *: boolean
          pdf *: boolean
          text *: boolean
          video *: boolean
        }
        reasoning *: boolean
        temperature *: boolean
        toolcall *: boolean
      }
      cost *: Object {
        cache *: Object {
          read *: number
          write *: number
        }
        experimentalOver200K: Object {
          cache *: Object {
            read *: number
            write *: number
          }
          input *: number
          output *: number
        }
        input *: number
        output *: number
        tiers: Array<Object {
          cache *: Object {
            read *: number
            write *: number
          }
          input *: number
          output *: number
          tier *: Object {
            size *: number
            type *: Enum[context]
          }
        }>
      }
      family: string
      headers *: Object{}
      id *: string
      limit *: Object {
        context *: number
        input: number
        output *: number
      }
      name *: string
      options *: Object{}
      providerID *: string
      release_date *: string
      status *: Enum[alpha, beta, deprecated, active]
      variants: Object{}
    }


  ModelV2Info
  ~~~~~~~~~~~
Object {
      apiID *: string
      capabilities *: Object {
        input *: Array<string>
        output *: Array<string>
        tools *: boolean
      }
      cost *: Array<Object {
        cache *: Object {
          read *: number
          write *: number
        }
        input *: number
        output *: number
        tier: Object {
          size *: integer
          type *: Enum[context]
        }
      }>
      enabled *: boolean
      endpoint *: Union(Object {
        type *: Enum[unknown]
      }, Object {
        type *: Enum[openai/responses]
        url *: string
        websocket: boolean
      }, Object {
        reasoning: Union(Object {
          type *: Enum[reasoning_content]
        }, Object {
          type *: Enum[reasoning_details]
        })
        type *: Enum[openai/completions]
        url *: string
      }, Object {
        type *: Enum[anthropic/messages]
        url *: string
      }, Object {
        package *: string
        type *: Enum[aisdk]
        url: string
      })
      family: string
      id *: string
      limit *: Object {
        context *: integer
        input: integer
        output *: integer
      }
      name *: string
      options *: Object {
        aisdk *: Object {
          provider *: Object{}
          request *: Object{}
        }
        body *: Object{}
        headers *: Object{}
        variant: string
      }
      providerID *: string
      status *: Enum[alpha, beta, deprecated, active]
      time *: Object {
        released *: Union(number, Enum[NaN], Enum[Infinity], Enum[-Infinity], Enum[Infinity, -Infinity, NaN])
      }
      variants *: Array<Object {
        aisdk *: Object {
          provider *: Object{}
          request *: Object{}
        }
        body *: Object{}
        headers *: Object{}
        id *: string
      }>
    }


  ModelV2Info1
  ~~~~~~~~~~~~
Object {
      apiID *: string
      capabilities *: Object {
        input *: Array<string>
        output *: Array<string>
        tools *: boolean
      }
      cost *: Array<Object {
        cache *: Object {
          read *: number
          write *: number
        }
        input *: number
        output *: number
        tier: Object {
          size *: integer
          type *: Enum[context]
        }
      }>
      enabled *: boolean
      endpoint *: Union(Object {
        type *: Enum[unknown]
      }, Object {
        type *: Enum[openai/responses]
        url *: string
        websocket: boolean
      }, Object {
        reasoning: Union(Object {
          type *: Enum[reasoning_content]
        }, Object {
          type *: Enum[reasoning_details]
        })
        type *: Enum[openai/completions]
        url *: string
      }, Object {
        type *: Enum[anthropic/messages]
        url *: string
      }, Object {
        package *: string
        type *: Enum[aisdk]
        url: string
      })
      family: string
      id *: string
      limit *: Object {
        context *: integer
        input: integer
        output *: integer
      }
      name *: string
      options *: Object {
        aisdk *: Object {
          provider *: Object{}
          request *: Object{}
        }
        body *: Object{}
        headers *: Object{}
        variant: string
      }
      providerID *: string
      status *: Enum[alpha, beta, deprecated, active]
      time *: Object {
        released *: Union(number, Enum[NaN], Enum[Infinity], Enum[-Infinity])
      }
      variants *: Array<Object {
        aisdk *: Object {
          provider *: Object{}
          request *: Object{}
        }
        body *: Object{}
        headers *: Object{}
        id *: string
      }>
    }


  NotFoundError
  ~~~~~~~~~~~~~
Object {
      data *: Object {
        message *: string
      }
      name *: Enum[NotFoundError]
    }


  OAuth
  ~~~~~
Object {
      access *: string
      accountId: string
      enterpriseUrl: string
      expires *: integer
      refresh *: string
      type *: Enum[oauth]
    }


  OutputFormat
  ~~~~~~~~~~~~
Union(OutputFormatText, OutputFormatJsonSchema)


  OutputFormatJsonSchema
  ~~~~~~~~~~~~~~~~~~~~~~
Object {
      retryCount: integer
      schema *: JSONSchema
      type *: Enum[json_schema]
    }


  OutputFormatText
  ~~~~~~~~~~~~~~~~
Object {
      type *: Enum[text]
    }


  Part
  ~~~~
Union(TextPart, SubtaskPart, ReasoningPart, FilePart, ToolPart) ...(+7 more)


  PatchPart
  ~~~~~~~~~
Object {
      files *: Array<string>
      hash *: string
      id *: string
      messageID *: string
      sessionID *: string
      type *: Enum[patch]
    }


  Path
  ~~~~
Object {
      config *: string
      directory *: string
      home *: string
      state *: string
      worktree *: string
    }


  PermissionAction
  ~~~~~~~~~~~~~~~~
Enum[allow, deny, ask]


  PermissionActionConfig
  ~~~~~~~~~~~~~~~~~~~~~~
Enum[ask, allow, deny]


  PermissionConfig
  ~~~~~~~~~~~~~~~~
Union(PermissionActionConfig, Object {
      bash: PermissionRuleConfig
      doom_loop: PermissionActionConfig
      edit: PermissionRuleConfig
      external_directory: PermissionRuleConfig
      glob: PermissionRuleConfig
      grep: PermissionRuleConfig
      list: PermissionRuleConfig
      lsp: PermissionRuleConfig
      question: PermissionActionConfig
      read: PermissionRuleConfig
      repo_clone: PermissionRuleConfig
      repo_overview: PermissionRuleConfig
      skill: PermissionRuleConfig
      task: PermissionRuleConfig
      todowrite: PermissionActionConfig
      webfetch: PermissionActionConfig
      websearch: PermissionActionConfig
    })


  PermissionObjectConfig
  ~~~~~~~~~~~~~~~~~~~~~~
Object{}


  PermissionRequest
  ~~~~~~~~~~~~~~~~~
Object {
      always *: Array<string>
      id *: string
      metadata *: Object{}
      patterns *: Array<string>
      permission *: string
      sessionID *: string
      tool: Object {
        callID *: string
        messageID *: string
      }
    }


  PermissionRule
  ~~~~~~~~~~~~~~
Object {
      action *: PermissionAction
      pattern *: string
      permission *: string
    }


  PermissionRuleConfig
  ~~~~~~~~~~~~~~~~~~~~
Union(PermissionActionConfig, PermissionObjectConfig)


  PermissionRuleset
  ~~~~~~~~~~~~~~~~~
Array<PermissionRule>


  Project
  ~~~~~~~
Object {
      commands: Object {
        start: string  # Startup script to run when creating a new workspace (worktree)
      }
      icon: Object {
        color: string
        override: string
        url: string
      }
      id *: string
      name: string
      sandboxes *: Array<string>
      time *: Object {
        created *: integer
        initialized: integer
        updated *: integer
      }
      vcs: Enum[git]
      worktree *: string
    }


  ProjectSummary
  ~~~~~~~~~~~~~~
Object {
      id *: string
      name: string
      worktree *: string
    }


  Prompt
  ~~~~~~
Object {
      agents: Array<PromptAgentAttachment>
      files: Array<PromptFileAttachment>
      references: Array<PromptReferenceAttachment>
      text *: string
    }


  PromptAgentAttachment
  ~~~~~~~~~~~~~~~~~~~~~
Object {
      name *: string
      source: PromptSource
    }


  PromptFileAttachment
  ~~~~~~~~~~~~~~~~~~~~
Object {
      description: string
      mime *: string
      name: string
      source: PromptSource
      uri *: string
    }


  PromptReferenceAttachment
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      branch: string
      kind *: Enum[local, git, invalid]
      name *: string
      problem: string
      repository: string
      source: PromptSource
      target: string
      targetUri: string
      uri: string
    }


  PromptSource
  ~~~~~~~~~~~~
Object {
      end *: number
      start *: number
      text *: string
    }


  Provider
  ~~~~~~~~
Object {
      env *: Array<string>
      id *: string
      key: string
      models *: Object{}
      name *: string
      options *: Object{}
      source *: Enum[env, config, custom, api]
    }


  ProviderAuthAuthorization
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      instructions *: string
      method *: Enum[auto, code]
      url *: string
    }


  ProviderAuthError
  ~~~~~~~~~~~~~~~~~
Object {
      data *: Object {
        message *: string
        providerID *: string
      }
      name *: Enum[ProviderAuthError]
    }


  ProviderAuthError1
  ~~~~~~~~~~~~~~~~~~
Object {
      data *: Object {
        field: string
        kind: string
        message: string
        providerID: string
      }
      name *: Enum[BadRequest, ProviderAuthOauthMissing, ProviderAuthOauthCodeMissing, ProviderAuthOauthCallbackFailed, ProviderAuthValidationFailed]
    }


  ProviderAuthMethod
  ~~~~~~~~~~~~~~~~~~
Object {
      label *: string
      prompts: Array<Union(Object {
        key *: string
        message *: string
        placeholder: string
        type *: Enum[text]
        when: Object {
          key *: string
          op *: Enum[eq, neq]
          value *: string
        }
      }, Object {
        key *: string
        message *: string
        options *: Array<Object {
          hint: string
          label *: string
          value *: string
        }>
        type *: Enum[select]
        when: Object {
          key *: string
          op *: Enum[eq, neq]
          value *: string
        }
      })>
      type *: Enum[oauth, api]
    }


  ProviderConfig
  ~~~~~~~~~~~~~~
Object {
      api: string
      blacklist: Array<string>
      env: Array<string>
      id: string
      models: Object{}
      name: string
      npm: string
      options: Object {
        apiKey: string
        baseURL: string
        chunkTimeout: integer
        enterpriseUrl: string
        setCacheKey: boolean
        timeout: Union(integer, boolean)  # Timeout in milliseconds for requests to this provider. Default is 300000 (5 minutes). Set to false to disable timeout.
      }
      whitelist: Array<string>
    }


  ProviderV2Info
  ~~~~~~~~~~~~~~
Object {
      enabled *: Union(boolean, Object {
        name *: string
        via *: Enum[env]
      }, Object {
        service *: string
        via *: Enum[auth]
      }, Object {
        data *: Object{}
        via *: Enum[custom]
      })
      endpoint *: Union(Object {
        type *: Enum[unknown]
      }, Object {
        type *: Enum[openai/responses]
        url *: string
        websocket: boolean
      }, Object {
        reasoning: Union(Object {
          type *: Enum[reasoning_content]
        }, Object {
          type *: Enum[reasoning_details]
        })
        type *: Enum[openai/completions]
        url *: string
      }, Object {
        type *: Enum[anthropic/messages]
        url *: string
      }, Object {
        package *: string
        type *: Enum[aisdk]
        url: string
      })
      env *: Array<string>
      id *: string
      name *: string
      options *: Object {
        aisdk *: Object {
          provider *: Object{}
          request *: Object{}
        }
        body *: Object{}
        headers *: Object{}
      }
    }


  Pty
  ~~~
Object {
      args *: Array<string>
      command *: string
      cwd *: string
      id *: string
      pid *: integer
      status *: Enum[running, exited]
      title *: string
    }


  QuestionAnswer
  ~~~~~~~~~~~~~~
Array<string>


  QuestionInfo
  ~~~~~~~~~~~~
Object {
      custom: boolean
      header *: string  # Very short label (max 30 chars)
      multiple: boolean
      options *: Array<QuestionOption>  # Available choices
      question *: string  # Complete question
    }


  QuestionOption
  ~~~~~~~~~~~~~~
Object {
      description *: string  # Explanation of choice
      label *: string  # Display text (1-5 words, concise)
    }


  QuestionRejected
  ~~~~~~~~~~~~~~~~
Object {
      requestID *: string
      sessionID *: string
    }


  QuestionReplied
  ~~~~~~~~~~~~~~~
Object {
      answers *: Array<QuestionAnswer>
      requestID *: string
      sessionID *: string
    }


  QuestionRequest
  ~~~~~~~~~~~~~~~
Object {
      id *: string
      questions *: Array<QuestionInfo>  # Questions to ask
      sessionID *: string
      tool: QuestionTool
    }


  QuestionTool
  ~~~~~~~~~~~~
Object {
      callID *: string
      messageID *: string
    }


  Range
  ~~~~~
Object {
      end *: Object {
        character *: integer
        line *: integer
      }
      start *: Object {
        character *: integer
        line *: integer
      }
    }


  ReasoningPart
  ~~~~~~~~~~~~~
Object {
      id *: string
      messageID *: string
      metadata: Object{}
      sessionID *: string
      text *: string
      time *: Object {
        end: integer
        start *: integer
      }
      type *: Enum[reasoning]
    }


  ReferenceConfig
  ~~~~~~~~~~~~~~~
Object{}


  ReferenceConfigEntry
  ~~~~~~~~~~~~~~~~~~~~
Union(string, Object {
      branch: string
      repository *: string  # Git repository URL, host/path reference, or GitHub owner/repo shorthand
    }, Object {
      path *: string  # Absolute path, ~/ path, or workspace-relative path to a local reference directory
    })


  ResourceSource
  ~~~~~~~~~~~~~~
Object {
      clientName *: string
      text *: FilePartSourceText
      type *: Enum[resource]
      uri *: string
    }


  RetryPart
  ~~~~~~~~~
Object {
      attempt *: integer
      error *: APIError
      id *: string
      messageID *: string
      sessionID *: string
      time *: Object {
        created *: integer
      }
      type *: Enum[retry]
    }


  ServerConfig
  ~~~~~~~~~~~~
    Server configuration for opencode serve and web commands
Object {
      cors: Array<string>
      hostname: string
      mdns: boolean
      mdnsDomain: string
      port: integer
    }


  Session
  ~~~~~~~
Object {
      agent: string
      cost: number
      directory *: string
      id *: string
      model: Object {
        id *: string
        providerID *: string
        variant: string
      }
      parentID: string
      path: string
      permission: PermissionRuleset
      projectID *: string
      revert: Object {
        diff: string
        messageID *: string
        partID: string
        snapshot: string
      }
      share: Object {
        url *: string
      }
      slug *: string
      summary: Object {
        additions *: number
        deletions *: number
        diffs: Array<SnapshotFileDiff>
        files *: number
      }
      time *: Object {
        archived: number
        compacting: integer
        created *: integer
        updated *: integer
      }
      title *: string
      tokens: Object {
        cache *: Object {
          read *: number
          write *: number
        }
        input *: number
        output *: number
        reasoning *: number
      }
      version *: string
      workspaceID: string
    }


  SessionDelivery
  ~~~~~~~~~~~~~~~
Enum[immediate, deferred]


  SessionErrorUnknown
  ~~~~~~~~~~~~~~~~~~~
Object {
      message *: string
      type *: Enum[unknown]
    }


  SessionInfo
  ~~~~~~~~~~~
Object {
      agent: string
      cost *: number
      id *: string
      model: Object {
        id *: string
        providerID *: string
        variant *: string
      }
      parentID: string
      path: string
      projectID *: string
      time *: Object {
        archived: number
        created *: number
        updated *: number
      }
      title *: string
      tokens *: Object {
        cache *: Object {
          read *: number
          write *: number
        }
        input *: number
        output *: number
        reasoning *: number
      }
      workspaceID: string
    }


  SessionMessage
  ~~~~~~~~~~~~~~
Union(SessionMessageAgentSwitched, SessionMessageModelSwitched, SessionMessageUser, SessionMessageSynthetic, SessionMessageShell) ...(+2 more)


  SessionMessageAgentSwitched
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      agent *: string
      id *: string
      metadata: Object{}
      time *: Object {
        created *: number
      }
      type *: Enum[agent-switched]
    }


  SessionMessageAssistant
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      agent *: string
      content *: Array<Union(SessionMessageAssistantText, SessionMessageAssistantReasoning, SessionMessageAssistantTool)>
      cost: number
      error: SessionErrorUnknown
      finish: string
      id *: string
      metadata: Object{}
      model *: Object {
        id *: string
        providerID *: string
        variant *: string
      }
      snapshot: Object {
        end: string
        start: string
      }
      time *: Object {
        completed: number
        created *: number
      }
      tokens: Object {
        cache *: Object {
          read *: number
          write *: number
        }
        input *: number
        output *: number
        reasoning *: number
      }
      type *: Enum[assistant]
    }


  SessionMessageAssistantReasoning
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      text *: string
      type *: Enum[reasoning]
    }


  SessionMessageAssistantText
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      text *: string
      type *: Enum[text]
    }


  SessionMessageAssistantTool
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      name *: string
      provider: Object {
        executed *: boolean
        metadata: Object{}
      }
      state *: Union(SessionMessageToolStatePending, SessionMessageToolStateRunning, SessionMessageToolStateCompleted, SessionMessageToolStateError)
      time *: Object {
        completed: number
        created *: number
        pruned: number
        ran: number
      }
      type *: Enum[tool]
    }


  SessionMessageCompaction
  ~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      include: string
      metadata: Object{}
      reason *: Enum[auto, manual]
      summary *: string
      time *: Object {
        created *: number
      }
      type *: Enum[compaction]
    }


  SessionMessageModelSwitched
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      metadata: Object{}
      model *: Object {
        id *: string
        providerID *: string
        variant *: string
      }
      time *: Object {
        created *: number
      }
      type *: Enum[model-switched]
    }


  SessionMessageShell
  ~~~~~~~~~~~~~~~~~~~
Object {
      callID *: string
      command *: string
      id *: string
      metadata: Object{}
      output *: string
      time *: Object {
        completed: number
        created *: number
      }
      type *: Enum[shell]
    }


  SessionMessageSynthetic
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      id *: string
      metadata: Object{}
      sessionID *: string
      text *: string
      time *: Object {
        created *: number
      }
      type *: Enum[synthetic]
    }


  SessionMessageToolStateCompleted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      attachments: Array<PromptFileAttachment>
      content *: Array<Union(ToolTextContent, ToolFileContent)>
      input *: Object{}
      status *: Enum[completed]
      structured *: Object{}
    }


  SessionMessageToolStateError
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      content *: Array<Union(ToolTextContent, ToolFileContent)>
      error *: SessionErrorUnknown
      input *: Object{}
      status *: Enum[error]
      structured *: Object{}
    }


  SessionMessageToolStatePending
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      input *: string
      status *: Enum[pending]
    }


  SessionMessageToolStateRunning
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      content *: Array<Union(ToolTextContent, ToolFileContent)>
      input *: Object{}
      status *: Enum[running]
      structured *: Object{}
    }


  SessionMessageUser
  ~~~~~~~~~~~~~~~~~~
Object {
      agents: Array<PromptAgentAttachment>
      files: Array<PromptFileAttachment>
      id *: string
      metadata: Object{}
      references: Array<PromptReferenceAttachment>
      text *: string
      time *: Object {
        created *: number
      }
      type *: Enum[user]
    }


  SessionNextRetry_error
  ~~~~~~~~~~~~~~~~~~~~~~
Object {
      isRetryable *: boolean
      message *: string
      metadata: Object{}
      responseBody: string
      responseHeaders: Object{}
      statusCode: number
    }


  SessionStatus
  ~~~~~~~~~~~~~
Union(Object {
      type *: Enum[idle]
    }, Object {
      action: Object {
        label *: string
        link: string
        message *: string
        provider *: string
        reason *: string
        title *: string
      }
      attempt *: integer
      message *: string
      next *: integer
      type *: Enum[retry]
    }, Object {
      type *: Enum[busy]
    })


  SnapshotFileDiff
  ~~~~~~~~~~~~~~~~
Object {
      additions *: number
      deletions *: number
      file: string
      patch: string
      status: Enum[added, deleted, modified]
    }


  SnapshotPart
  ~~~~~~~~~~~~
Object {
      id *: string
      messageID *: string
      sessionID *: string
      snapshot *: string
      type *: Enum[snapshot]
    }


  StepFinishPart
  ~~~~~~~~~~~~~~
Object {
      cost *: number
      id *: string
      messageID *: string
      reason *: string
      sessionID *: string
      snapshot: string
      tokens *: Object {
        cache *: Object {
          read *: number
          write *: number
        }
        input *: number
        output *: number
        reasoning *: number
        total: number
      }
      type *: Enum[step-finish]
    }


  StepStartPart
  ~~~~~~~~~~~~~
Object {
      id *: string
      messageID *: string
      sessionID *: string
      snapshot: string
      type *: Enum[step-start]
    }


  StructuredOutputError
  ~~~~~~~~~~~~~~~~~~~~~
Object {
      data *: Object {
        message *: string
        retries *: integer
      }
      name *: Enum[StructuredOutputError]
    }


  SubtaskPart
  ~~~~~~~~~~~
Object {
      agent *: string
      command: string
      description *: string
      id *: string
      messageID *: string
      model: Object {
        modelID *: string
        providerID *: string
      }
      prompt *: string
      sessionID *: string
      type *: Enum[subtask]
    }


  SubtaskPartInput
  ~~~~~~~~~~~~~~~~
Object {
      agent *: string
      command: string
      description *: string
      id: string
      model: Object {
        modelID *: string
        providerID *: string
      }
      prompt *: string
      type *: Enum[subtask]
    }


  Symbol
  ~~~~~~
Object {
      kind *: integer
      location *: Object {
        range *: Range
        uri *: string
      }
      name *: string
    }


  SymbolSource
  ~~~~~~~~~~~~
Object {
      kind *: integer
      name *: string
      path *: string
      range *: Range
      text *: FilePartSourceText
      type *: Enum[symbol]
    }


  SyncEventMessagePartRemoved
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        messageID *: string
        partID *: string
        sessionID *: string
      }
      id *: string
      name *: Enum[message.part.removed.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventMessagePartUpdated
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        part *: Part
        sessionID *: string
        time *: integer
      }
      id *: string
      name *: Enum[message.part.updated.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventMessageRemoved
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        messageID *: string
        sessionID *: string
      }
      id *: string
      name *: Enum[message.removed.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventMessageUpdated
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        info *: Message
        sessionID *: string
      }
      id *: string
      name *: Enum[message.updated.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionCreated
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        info *: Session
        sessionID *: string
      }
      id *: string
      name *: Enum[session.created.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionDeleted
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        info *: Session
        sessionID *: string
      }
      id *: string
      name *: Enum[session.deleted.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextAgentSwitched
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        agent *: string
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.agent.switched.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextCompactionDelta
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        sessionID *: string
        text *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.compaction.delta.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextCompactionEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        include: string
        sessionID *: string
        text *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.compaction.ended.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextCompactionStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        reason *: Enum[auto, manual]
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.compaction.started.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextModelSwitched
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        model *: Object {
          id *: string
          providerID *: string
          variant *: string
        }
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.model.switched.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextPrompted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        prompt *: Prompt
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.prompted.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextReasoningDelta
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        delta *: string
        reasoningID *: string
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.reasoning.delta.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextReasoningEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        reasoningID *: string
        sessionID *: string
        text *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.reasoning.ended.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextReasoningStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        reasoningID *: string
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.reasoning.started.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextRetried
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        attempt *: number
        error *: SessionNextRetry_error
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.retried.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextShellEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        callID *: string
        output *: string
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.shell.ended.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextShellStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        callID *: string
        command *: string
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.shell.started.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextStepEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        cost *: number
        finish *: string
        sessionID *: string
        snapshot: string
        timestamp *: number
        tokens *: Object {
          cache *: Object {
            read *: number
            write *: number
          }
          input *: number
          output *: number
          reasoning *: number
        }
      }
      id *: string
      name *: Enum[session.next.step.ended.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextStepFailed
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        error *: SessionErrorUnknown
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.step.failed.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextStepStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        agent *: string
        model *: Object {
          id *: string
          providerID *: string
          variant *: string
        }
        sessionID *: string
        snapshot: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.step.started.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextSynthetic
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        sessionID *: string
        text *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.synthetic.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextTextDelta
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        delta *: string
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.text.delta.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextTextEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        sessionID *: string
        text *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.text.ended.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextTextStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.text.started.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextToolCalled
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        callID *: string
        input *: Object{}
        provider *: Object {
          executed *: boolean
          metadata: Object{}
        }
        sessionID *: string
        timestamp *: number
        tool *: string
      }
      id *: string
      name *: Enum[session.next.tool.called.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextToolFailed
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        callID *: string
        error *: SessionErrorUnknown
        provider *: Object {
          executed *: boolean
          metadata: Object{}
        }
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.tool.failed.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextToolInputDelta
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        callID *: string
        delta *: string
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.tool.input.delta.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextToolInputEnded
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        callID *: string
        sessionID *: string
        text *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.tool.input.ended.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextToolInputStarted
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        callID *: string
        name *: string
        sessionID *: string
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.tool.input.started.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextToolProgress
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        callID *: string
        content *: Array<Union(ToolTextContent, ToolFileContent)>
        sessionID *: string
        structured *: Object{}
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.tool.progress.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionNextToolSuccess
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        callID *: string
        content *: Array<Union(ToolTextContent, ToolFileContent)>
        provider *: Object {
          executed *: boolean
          metadata: Object{}
        }
        sessionID *: string
        structured *: Object{}
        timestamp *: number
      }
      id *: string
      name *: Enum[session.next.tool.success.1]
      seq *: number
      type *: Enum[sync]
    }


  SyncEventSessionUpdated
  ~~~~~~~~~~~~~~~~~~~~~~~
Object {
      aggregateID *: Enum[sessionID]
      data *: Object {
        info *: Object {
          agent: Union(string, null)
          cost: Union(number, null)
          directory: Union(string, null)
          id: Union(string, null)
          model: Union(Object {
            id *: string
            providerID *: string
            variant: string
          }, null)
          parentID: Union(string, null)
          path: Union(string, null)
          permission: Union(PermissionRuleset, null)
          projectID: Union(string, null)
          revert: Union(Object {
            diff: string
            messageID *: string
            partID: string
            snapshot: string
          }, null)
          share: Object {
            url: Union(string, null)
          }
          slug: Union(string, null)
          summary: Union(Object {
            additions *: number
            deletions *: number
            diffs: Array<SnapshotFileDiff>
            files *: number
          }, null)
          time: Object {
            archived: Union(number, null)
            compacting: Union(integer, null)
            created: Union(integer, null)
            updated: Union(integer, null)
          }
          title: Union(string, null)
          tokens: Union(Object {
            cache *: Object {
              read *: number
              write *: number
            }
            input *: number
            output *: number
            reasoning *: number
          }, null)
          version: Union(string, null)
          workspaceID: Union(string, null)
        }
        sessionID *: string
      }
      id *: string
      name *: Enum[session.updated.1]
      seq *: number
      type *: Enum[sync]
    }


  TextPart
  ~~~~~~~~
Object {
      id *: string
      ignored: boolean
      messageID *: string
      metadata: Object{}
      sessionID *: string
      synthetic: boolean
      text *: string
      time: Object {
        end: integer
        start *: integer
      }
      type *: Enum[text]
    }


  TextPartInput
  ~~~~~~~~~~~~~
Object {
      id: string
      ignored: boolean
      metadata: Object{}
      synthetic: boolean
      text *: string
      time: Object {
        end: integer
        start *: integer
      }
      type *: Enum[text]
    }


  Todo
  ~~~~
Object {
      content *: string  # Brief description of the task
      priority *: string  # Priority level of the task: high, medium, low
      status *: string  # Current status of the task: pending, in_progress, completed, cancelled
    }


  ToolFileContent
  ~~~~~~~~~~~~~~~
Object {
      mime *: string
      name: string
      type *: Enum[file]
      uri *: string
    }


  ToolIDs
  ~~~~~~~
Array<string>


  ToolList
  ~~~~~~~~
Array<ToolListItem>


  ToolListItem
  ~~~~~~~~~~~~
Object {
      description *: string
      id *: string
      parameters *: Object{}
    }


  ToolPart
  ~~~~~~~~
Object {
      callID *: string
      id *: string
      messageID *: string
      metadata: Object{}
      sessionID *: string
      state *: ToolState
      tool *: string
      type *: Enum[tool]
    }


  ToolState
  ~~~~~~~~~
Union(ToolStatePending, ToolStateRunning, ToolStateCompleted, ToolStateError)


  ToolStateCompleted
  ~~~~~~~~~~~~~~~~~~
Object {
      attachments: Array<FilePart>
      input *: Object{}
      metadata *: Object{}
      output *: string
      status *: Enum[completed]
      time *: Object {
        compacted: integer
        end *: integer
        start *: integer
      }
      title *: string
    }


  ToolStateError
  ~~~~~~~~~~~~~~
Object {
      error *: string
      input *: Object{}
      metadata: Object{}
      status *: Enum[error]
      time *: Object {
        end *: integer
        start *: integer
      }
    }


  ToolStatePending
  ~~~~~~~~~~~~~~~~
Object {
      input *: Object{}
      raw *: string
      status *: Enum[pending]
    }


  ToolStateRunning
  ~~~~~~~~~~~~~~~~
Object {
      input *: Object{}
      metadata: Object{}
      status *: Enum[running]
      time *: Object {
        start *: integer
      }
      title: string
    }


  ToolTextContent
  ~~~~~~~~~~~~~~~
Object {
      text *: string
      type *: Enum[text]
    }


  UnknownError
  ~~~~~~~~~~~~
Object {
      data *: Object {
        message *: string
      }
      name *: Enum[UnknownError]
    }


  UserMessage
  ~~~~~~~~~~~
Object {
      agent *: string
      format: OutputFormat
      id *: string
      model *: Object {
        modelID *: string
        providerID *: string
        variant: string
      }
      role *: Enum[user]
      sessionID *: string
      summary: Object {
        body: string
        diffs *: Array<SnapshotFileDiff>
        title: string
      }
      system: string
      time *: Object {
        created *: integer
      }
      tools: Object{}
    }


  V2SessionMessagesResponse
  ~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      cursor *: Object {
        next: string
        previous: string
      }
      items *: Array<SessionMessage>
    }


  V2SessionsResponse
  ~~~~~~~~~~~~~~~~~~
Object {
      cursor *: Object {
        next: string
        previous: string
      }
      items *: Array<SessionInfo>
    }


  VcsApplyError
  ~~~~~~~~~~~~~
Object {
      data *: Object {
        message *: string
        reason *: Enum[non-git, not-clean]
      }
      name *: Enum[VcsApplyError]
    }


  VcsFileDiff
  ~~~~~~~~~~~
Object {
      additions *: number
      deletions *: number
      file *: string
      patch: string
      status: Enum[added, deleted, modified]
    }


  VcsFileStatus
  ~~~~~~~~~~~~~
Object {
      additions *: number
      deletions *: number
      file *: string
      status *: Enum[added, deleted, modified]
    }


  VcsInfo
  ~~~~~~~
Object {
      branch: string
      default_branch: string
    }


  WellKnownAuth
  ~~~~~~~~~~~~~
Object {
      key *: string
      token *: string
      type *: Enum[wellknown]
    }


  Workspace
  ~~~~~~~~~
Object {
      branch: Union(string, null)
      directory: Union(string, null)
      extra: Union(Object{}, null)
      id *: string
      name *: string
      projectID *: string
      timeUsed *: Union(number, Enum[NaN], Enum[Infinity], Enum[-Infinity], Enum[Infinity, -Infinity, NaN])
      type *: string
    }


  WorkspaceWarpError
  ~~~~~~~~~~~~~~~~~~
Object {
      data *: Object {
        message *: string
      }
      name *: Enum[WorkspaceWarpError]
    }


  Worktree
  ~~~~~~~~
Object {
      branch: string
      directory *: string
      name *: string
    }


  WorktreeCreateInput
  ~~~~~~~~~~~~~~~~~~~
Object {
      name: string
      startCommand: string  # Additional startup script to run after the project's start command
    }


  WorktreeError
  ~~~~~~~~~~~~~
Object {
      data *: Object {
        message *: string
      }
      name *: Enum[WorktreeNotGitError, WorktreeNameGenerationFailedError, WorktreeCreateFailedError, WorktreeStartCommandFailedError, WorktreeRemoveFailedError, WorktreeResetFailedError, WorktreeListFailedError]
    }


  WorktreeRemoveInput
  ~~~~~~~~~~~~~~~~~~~
Object {
      directory *: string
    }


  WorktreeResetInput
  ~~~~~~~~~~~~~~~~~~
Object {
      directory *: string
    }


  effect_HttpApiError_Forbidden
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      _tag *: Enum[Forbidden]
    }


  effect_HttpApiError_InternalServerError
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Object {
      _tag *: Enum[InternalServerError]
    }

================================================================================
  Generated from OpenCode OpenAPI spec v1.0.0
================================================================================